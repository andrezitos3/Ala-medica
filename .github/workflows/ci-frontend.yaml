name: CI frontend

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-angular:
  
    runs-on: server01

    steps:
    # Faz o checkout do código
    - uses: actions/checkout@v4

    # Setup Node.js
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: ./FrontEnd/package-lock.json

    # Instala dependências
    - name: Install dependencies
      run: npm install
      working-directory: ./FrontEnd

    # build do projeto Angular
    - name: Build
      run: npm run build
      working-directory: ./FrontEnd

    # testes unitários (ver depois)
    #- name: Run tests
     # run: npm run test
      #working-directory: ./FrontEnd
